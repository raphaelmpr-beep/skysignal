# SkySignal API — Python dependencies
# Python 3.11+

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
alembic==1.13.1
geoalchemy2==0.15.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# HTTP client (for geocoding, GDELT)
httpx==0.27.0

# Pydantic v2 (bundled with FastAPI 0.111+, listed explicitly)
pydantic[email]==2.7.1
pydantic-settings==2.3.0

# PDF generation (optional — falls back gracefully if not installed)
weasyprint==62.2

# Utilities
python-dotenv==1.0.1
